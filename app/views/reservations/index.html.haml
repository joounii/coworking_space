%p{:style => "color: green"}= notice
%h1 Reservations
%table.table.table-hover.table-striped
    %thead
        %tr
            %th{:scope => "col"} Id
            %th{:scope => "col"} Reservation Date
            %th{:scope => "col"} Seat
    %tbody
        - @reservations.each do |reservation|
            %tr{:onclick => "location.href='#{reservation_path(reservation)}'"}
                %td= reservation.id
                %td= reservation.reservation_date
                %td= reservation.seat.title

= link_to "New reservation", new_reservation_path, class: "btn btn-primary"